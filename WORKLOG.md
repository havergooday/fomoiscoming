# FOMO — 작업 문서

> Fear Of Missing Out · 주식 그래프 크래시 도박 미니게임  
> GitHub Pages: https://havergooday.github.io/fomoiscoming/

---

## 컨셉 요약

BUY를 누르면 주가 그래프가 실시간으로 오르기 시작한다.  
배수(multiplier)가 높아질수록 수익이 커지지만, 언제든 그래프가 0으로 수직 폭락할 수 있다.  
폭락 전에 SELL을 눌러야 이긴다. 폭락 타이밍은 BUY 순간 이미 결정되어 있고 플레이어는 알 수 없다.  
SELL 이후에도 그래프는 멈추지 않고 계속 올라가다가 폭락한다 → "더 늦게 팔걸" 후회 유발.

---

## 파일 구조

```
fomoiscoming/
└── index.html   # 게임 전체 (HTML + CSS + JS 단일 파일)
```

---

## v0.1 프로토타입 현황

### 구현 완료

| 항목 | 내용 |
|------|------|
| 초기 잔고 | $1,000 |
| 차트 렌더링 | Canvas 2D, 50ms 틱, 최근 220포인트 스크롤 |
| 배수 증가 | 틱당 기본 1.5% × 랜덤 노이즈 (±0.5%) |
| 폭락 분포 | 6% 즉사(1.00~1.07x) / 나머지 `1 / (random × 0.95)` 중앙값 ≈ 2x, 최대 100x |
| 차트 색상 | 초록=보유중 / 노랑=판매후(후회구간) / 빨강=폭락 낙하 |
| SELL 마커 | 판매 지점에 SOLD 라벨 + 노란 도트 |
| 빠른 베팅 | $10 / $50 / $100 / ALL IN |
| 이력 칩 | 최근 22라운드 수익(초록)/손실(빨강) 표시 |
| 상태 메시지 | 현재 배수 + 예상 수익 실시간 표시 |
| 반응형 | Canvas 리사이즈, max-width 560px |

### 기술 스택

- 순수 HTML / CSS / Vanilla JS
- 외부 라이브러리 없음
- GitHub Pages 단일 파일 배포

---

## 알려진 문제 / 한계

- [ ] **파산 후 게임 불가** — 잔고 0원 상태에서 새로고침 외에 재시작 방법 없음
- [ ] **배팅 소수점 미처리** — 입력값을 `Math.floor`로 정수화하지만 UX 안내 없음
- [ ] **즉사 시 SELL 버튼 반응 불가** — 첫 틱에 폭락하면 버튼이 켜지자마자 꺼짐
- [ ] **모바일 터치 반응 느림** — SELL 버튼 더블탭 시 확대되는 iOS 이슈
- [ ] **소리 없음** — 상승/폭락 피드백이 시각에만 의존
- [ ] **통계 없음** — 총 플레이 횟수, 최고 배수, 순손익 기록 안 됨

---

## 개선 아이디어

### 우선순위 높음

- [ ] **재시작 버튼** — 파산 시 $1,000으로 리셋하는 버튼 추가
- [ ] **사운드 피드백** — Web Audio API로 상승 틱음 + 폭락 효과음
- [ ] **자동베팅 방지 딜레이** — BUY 후 최소 0.5초간 SELL 버튼 비활성화 (즉사 라운드 UX 개선)

### 우선순위 중간

- [ ] **세션 통계 패널** — 라운드 수 / 최고배수 / 총 수익 표시
- [ ] **오토캐시아웃** — 목표 배수 지정 시 자동 판매
- [ ] **난이도 선택** — Easy(느린 상승, 낮은 즉사율) / Normal / Hard(빠른 상승, 높은 즉사율)

### 우선순위 낮음 (장기)

- [ ] **멀티플레이어 시뮬** — 다른 플레이어들의 판매 시점을 유령으로 표시
- [ ] **로컬스토리지 저장** — 잔고 / 이력 세션 유지
- [ ] **리더보드** — 최고 잔고 기록 (로컬 or GitHub Issues 활용)
- [ ] **모바일 앱 래핑** — PWA 등록 (manifest.json + service worker)

---

## 폭락 확률 분포 메모

```js
// BUY 시점에 1회 결정, 플레이어에게 비공개
function genCrashPoint() {
  if (Math.random() < 0.06) return 1 + Math.random() * 0.07; // 즉사 6%
  return Math.min(100, Math.max(1.05, 1 / (Math.random() * 0.95)));
}
```

| 구간 | 확률(이론) | 느낌 |
|------|-----------|------|
| ~1.07x | 6% | 즉사 |
| 1.07x ~ 2x | 44% | 허망한 손실 |
| 2x ~ 5x | 30% | 아슬아슬 |
| 5x ~ 20x | 15% | 흥분 |
| 20x ~ 100x | 5% | 전설 |

---

## 배수 증가 공식 메모

```
mult(t+1) = mult(t) × (1 + 0.015 + noise)
noise = (Math.random() - 0.5) × 0.01

틱 간격 50ms 기준
→ 1초 후 ≈ 1.35x
→ 5초 후 ≈ 2.1x
→ 10초 후 ≈ 4.5x
→ 20초 후 ≈ 20x
```

---

## 변경 이력

| 날짜 | 버전 | 내용 |
|------|------|------|
| 2026-05-14 | v0.1 | 프로토타입 최초 등록 |
